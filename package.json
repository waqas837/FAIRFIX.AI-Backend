{
  "name": "fairfix-backend",
  "version": "1.0.0",
  "description": "Backend API for FAIRFIX.AI (customer, shop owner, mechanic expert)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "create-admin": "node scripts/create-admin.js",
    "retention:run": "node -e \"require('./src/jobs/retention').runRetentionPurge().then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "express",
    "prisma",
    "postgresql",
    "vehicle-inspection"
  ],
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "crypto-js": "^4.2.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "jsonwebtoken": "^9.0.3",
    "node-cron": "^4.2.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "prisma": "^6.0.0"
  }
}
