{
  "name": "fairfix-backend",
  "version": "1.0.0",
  "description": "Backend API for FAIRFIX.AI (customer, shop owner, mechanic expert)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["express", "prisma", "postgresql", "vehicle-inspection"],
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
